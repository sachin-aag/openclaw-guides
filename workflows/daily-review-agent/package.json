{
  "name": "openclaw-daily-review-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Workshop Track A — daily review agent built on OpenClaw + Pi.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "openclaw dev --config gateway.config.yaml",
    "review": "openclaw skill run daily-review",
    "reload": "openclaw reload",
    "lint": "echo 'TODO: add eslint when source grows'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@openclaw/cli": "^0.x",
    "@openclaw/gateway": "^0.x",
    "@mariozechner/pi-coding-agent": "^0.73.0",
    "@mariozechner/pi-ai": "^0.73.0",
    "dotenv": "^16.4.5"
  }
}
