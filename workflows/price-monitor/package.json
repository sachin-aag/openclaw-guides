{
  "name": "openclaw-price-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Workshop Track B — price monitoring agent built on OpenClaw + Pi.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "openclaw dev --config gateway.config.yaml",
    "check": "openclaw skill run price-monitor",
    "reload": "openclaw reload"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@openclaw/cli": "^0.x",
    "@openclaw/gateway": "^0.x",
    "@mariozechner/pi-coding-agent": "^0.73.0",
    "@mariozechner/pi-ai": "^0.73.0",
    "dotenv": "^16.4.5"
  }
}
